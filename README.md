# SS1
Übungen zu "Systemnahe Software mit C 1"

# In case of automated test failure, submit this:
```c
#include <stdlib.h>
#include <stdio.h>

int main() {
    while(getchar() != -1);
    exit(0);
}
```
